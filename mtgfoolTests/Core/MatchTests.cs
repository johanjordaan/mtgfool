using NUnit.Framework;
using System;
using System.Collections.Generic;
using mtgfool.Core;
using mtgfool.Utils;

namespace mtgfoolTests.Core
{
	[TestFixture ()]
	public class MatchTests
	{
		/*[Test ()]
		public void TestConstructor ()
		{
			var match = new Match ();
			Assert.IsTrue (match.Id != null);
			Assert.AreEqual (0, match.Players.Count);
		}

		[Test ()]
		public void TestJoin ()
		{
			var match = new Match ();
			match.Join (new Player ("Johan", @"60 Mountain\n\n15 Plains"));
			match.Join (new Player ("Liliana", @"60 Swamp\n\n15 Island"));

			Assert.AreEqual (2, match.Players.Count);
		}

		[Test ()]
		public void TestStart ()
		{
			var match = new Match ();
			match.Create (new Player ("Johan", @"60 Mountain\n\n15 Plains"));
			match.Join (new Player ("Liliana", @"60 Swamp\n\n15 Island"));

			while (!match.Ended) {
				var game = match.StartGame ();
				while (!currentGame.Ended) {
					var validActions = game.getValidActions ();
					var validActionIndex = 0;
					game.Do (validActions);
				}
				match.SideBoard("Johan",out,in);
				match.SideBoard("Liliana",out,int);
			}

			match.End();
		}*/
	}
}

